import uuid
from datetime import datetime

trans_id = uuid.uuid4(),
date_initiated = datetime.now().fromisoformat,
# amount = amount,
# sender_acct_no = user.account_num,
# owner_id = user.id,

print(trans_id, date_initiated)